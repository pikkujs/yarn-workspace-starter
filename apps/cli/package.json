{
  "name": "@pikku-workspace-starter/cli",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "ncu": "ncu",
    "start": "tsx bin/cli.ts --websocket --fetch --rpc",
    "start:websocket": "tsx bin/cli.ts --websocket",
    "start:fetch": "tsx bin/cli.ts --fetch",
    "start:rpc": "tsx bin/cli.ts --rpc",
    "prebuild": "npx @pikku/cli fetch && npx @pikku/cli websocket && npx @pikku/cli rpc"
  },
  "dependencies": {
    "@pikku-workspace-starter/sdk": "*",
    "tsx": "^4.20.3"
  },
  "devDependencies": {
    "@types/node": "^24"
  }
}
