{
  "name": "@todos/cli",
  "version": "0.1.0",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "start": "ts-node bin/cli.ts",
    "start:websocket": "ts-node bin/cli.ts --websocket",
    "prebuild": "npx @vramework/cli fetch && npx @vramework/cli websocket"
  },
  "dependencies": {
    "@todos/sdk": "*",
    "ts-node": "^10.9.2"
  },
  "devDependencies": {
    "@types/node": "^22"
  }
}
